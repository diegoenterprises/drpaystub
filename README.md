## How to configure the app for development:
// For this you will need 2 terminal windows.

	a. First terminal:
		1. CD into root directory /drpaystub
		2. npm install
		3. npm start

	b. Second terminal:
		1. CD into /drpaystub/client
		2. npm install
		3. npm start